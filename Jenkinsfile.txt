pipeline {
    agent any
    stages {
        stage ('Clone') {
            steps {
                git branch: 'master', url: "https://github.com/reddykonda/SeleniumTestng.git"
            }
        }

        stage ('Exec Maven') {
            steps {
                rtMavenRun (
                    tool: maven_3_6_0, // Tool name from Jenkins configuration
                    pom: 'pom.xml',
                    goals: 'clean install'
                )
            }
        }

    }
}